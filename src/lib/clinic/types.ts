export type ClinicSettings = {
  legalName: string; tradeName: string; cnpj: string | null; logoUrl: string | null;
  email: string | null; phone: string | null; whatsapp: string | null;
  street: string | null; number: string | null; complement: string | null;
  district: string | null; city: string | null; state: string | null; postalCode: string | null;
  primaryColor: string; secondaryColor: string;
};
